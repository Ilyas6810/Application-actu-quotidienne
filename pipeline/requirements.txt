# Python 3.12 ou plus (numpy et scipy récents l'exigent).
feedparser>=6.0.11
requests>=2.32
PyYAML>=6.0.1
beautifulsoup4>=4.12
numpy>=1.26
scipy>=1.13
scikit-learn>=1.5
tzdata>=2024.1

# Vecteurs multilingues pour le regroupement : modèle de 220 Mo téléchargé au premier lancement.
# Absent ou en panne, le regroupement retombe sur TF-IDF seul.
fastembed>=0.5

# Tests
pytest>=8
